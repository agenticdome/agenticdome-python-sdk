from setuptools import setup


# Packaging metadata lives in pyproject.toml. Keep this shim for legacy
# tooling that still invokes setup.py directly without duplicating release
# metadata, dependency declarations, classifiers, or repository URLs.
setup()
